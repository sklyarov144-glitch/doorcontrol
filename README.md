# DoorControl

DoorControl - внутренняя веб-система строительной компании для контроля установки дверей на объектах.

MVP фокусируется на закрытой части системы: авторизация, навигация по строительной структуре объекта и учет состояния дверей, проемов, замечаний и актов ответственного хранения.

Публичный сайт-визитка компании не входит в MVP и будет добавлен позже отдельным этапом.

## Цель MVP

Сделать рабочую основу системы, в которой сотрудники смогут:

- войти по логину и паролю;
- видеть список объектов;
- переходить по иерархии объект -> корпус -> секция -> этаж -> дверь;
- просматривать и менять статусы дверей;
- вести учет проемов;
- фиксировать замечания;
- хранить данные по актам ответственного хранения;
- прикладывать файлы или фото только к актам ответственного хранения, если это необходимо по процессу.

## Стартовая структура

```text
DoorControl/
  index.html
  package.json
  README.md
  SPEC.md
  ROADMAP.md
  .gitignore
  docs/
    MVP_FLOW.md
    STATUSES.md
  src/
    backend/
    frontend/
    shared/
  tests/
```

## Запуск MVP-прототипа

Для запуска нужен Node.js.

```bash
npm install
npm run dev
```

После запуска Vite покажет локальный адрес приложения, обычно `http://127.0.0.1:5173/`.

Для проверки production-сборки:

```bash
npm run build
```

В прототипе используется простая мок-авторизация. На странице входа можно нажать `Войти` с уже подставленными демо-значениями.

## Предполагаемые модули

- Авторизация и роли пользователей.
- Реестр объектов.
- Иерархия строительства: корпуса, секции, этажи.
- Реестр дверей и их статусы.
- Учет проемов.
- Замечания и история их устранения.
- Акты ответственного хранения.
- Отчеты и выгрузки.

## Ограничения MVP

- Система является закрытой внутренней частью без публичного сайта.
- Файлы и фото используются только для актов ответственного хранения при необходимости.
- Сайт-визитка компании будет спроектирован и добавлен после запуска MVP.

## Статус проекта

Проект находится на этапе подготовки к разработке MVP. Сложный код и выбор финального технологического стека будут добавлены после согласования архитектуры закрытой части системы.
